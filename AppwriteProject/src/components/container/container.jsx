
function container({children}) {
  return <div className='w-full max-w-7xl mx-auto px-4'>
    {children}</div>
}

export default container
// In container we defind the height ,width basiaclly styling properties. 
// its accept the properties of children